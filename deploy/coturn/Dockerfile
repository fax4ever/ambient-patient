# Coturn TURN server image for WebRTC NAT traversal.
# Based on the same options as docs/turn-server.md (instrumentisto/coturn).
# Build and push to your registry, or use image "instrumentisto/coturn" directly in values.
FROM instrumentisto/coturn:latest
# Optional: pin a specific version, e.g. instrumentisto/coturn:4.5.2
# Default CMD from base image; override in deployment with full turnserver args.
